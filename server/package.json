{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for fibonacci application",
  "main": "index.js",
  "scripts": {
    "prod": "export NODE_ENV=production && nodemon index.js --ignore ./**/*.spec.*s",
    "start": "nodemon index.js --ignore ./**/*.spec.*s",
    "test": "export NODE_ENV=test && mocha ./**/*.spec.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "author": "Leonid Tsukanov",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "lodash": "^4.17.14",
    "mysql": "^2.18.1",
    "nodemon": "^1.19.1",
    "validator": "^11.1.0"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "chai": "^4.2.0",
    "cross-env": "^6.0.3",
    "mocha": "^10.2.0",
    "supertest": "^4.0.2"
  }
}
